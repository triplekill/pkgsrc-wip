$Id: TODO,v 1.1 2004/01/16 11:32:22 xtraeme Exp $

    o ffmpeg plugin (our current ffmpeg from pkgsrc is not enough)
    o VCD support in NetBSD (test)
    o Looks like current faad library in pkgsrc is not compatible?!
    o remove hard-coded "/dev/dsp"'s with DEVOSSAUDIO from
      ossaudio.buildlink2.mk 
    o test test test
