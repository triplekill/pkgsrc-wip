$Id$

* Ignores IPv6 flag
* Non-Posix pthread functions doesn't exist on *BSD
  ...patch-aa seems to be incorrect

--

I've actually it installed OK, but we haven't the functions
in -current "pthread_attr_{set,get}stacksize" at
this moment. 
